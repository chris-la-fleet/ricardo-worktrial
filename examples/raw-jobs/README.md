# Raw Jobs Examples

These demos are submitted directly with `kubectl apply`.

Current demos:

- `gpu-multinode-dtensor/`: 2-node GPU distributed smoke test using PyTorch distributed + DTensor.
- `rayjob-remote-task-fanout/`: RayJob demo that fans out Ray remote tasks and summarizes cluster resources.
