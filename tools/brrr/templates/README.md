# BRRR templates

Platform-owned manifest templates live in `tools/brrr`.

The current renderer builds YAML from structured Python objects to keep validation
and defaults in one place. This directory is reserved for future template
fragments as the rendering surface grows.
